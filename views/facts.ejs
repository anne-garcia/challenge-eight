<!DOCTYPE html>
<html>

<%- include('partials/head.ejs', { head }); -%>

<body>
    <h1 class="alignCentre">CAT FACTS</h1>

    <%- include('partials/nav.ejs', { user: body.user }); -%>

    <ul>
        <% for(const docFact of body.docArr_Facts) { %>
        <li class="margBot20">
            <a href="/facts/<%= docFact._id %>"><%= docFact.fact %></a>
        </li>
        <% } %>
    </ul>
</body>
</html>